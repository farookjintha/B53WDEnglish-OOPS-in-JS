let myName = "John";
let getName = () => {
  console.log("The name is ", myName);
};

let userDetails = {
  name: "Priya",
  email: "priya@gmail.com",
};

let fruits = ["apple", "mango", "lemon"];

// Everything in JS is an object (at last).

// Prototype -> is used to give all the details of the variable/function/object/class/array
